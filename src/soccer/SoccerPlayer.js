import { AbstractPlayer } from "../AbstractPlayer";
export class SoccerPlayer extends AbstractPlayer {
    /**
     *
     * @param {string} number
     * @param {string} name
     */
    constructor(number, name) {
        super();
        this.shirtNumber = number;
        this.shirtName = name;
    }
}
//# sourceMappingURL=SoccerPlayer.js.map